# SEO e GEO — o site encontrável e citável

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** fazer o site recém-virado ser encontrado pelos buscadores e citado corretamente pelos modelos generativos, com dados estruturados que descrevem o que a Alupar é e números com data de referência.

**Architecture:** quatro entregas independentes sobre o que já existe. O `robots.txt` passa a ser arquivo do repositório, com o ponteiro para o sitemap que o Astro já gera. Os dados estruturados saem de uma biblioteca pura e testada (`src/lib/seo.ts`), serializada pelo `Base.astro` — nenhuma lógica de schema dentro de template. O `llms.txt` é **gerado** do mapa de rotas por script com `--verificar` no CI, como os outros artefatos derivados deste repositório, para não envelhecer em silêncio. Os números institucionais viram dado em `src/lib/numeros.ts`, com data de observação, e alimentam o JSON-LD sem mudar uma linha do texto publicado.

**Tech Stack:** Astro 5 estático · TypeScript · `node --test` (Node 22, roda `.ts` sem compilador) · schema.org · Cloudflare Pages.

**Spec:** este plano implementa as proposições 1 a 4 da análise de 23/09/2026, registradas em [`docs/relatorio-de-entrega.md`](../../relatorio-de-entrega.md) (seções 5.4 e 8) e nos achados medidos em produção naquela data:

1. `https://www.alupar.com.br/robots.txt` serve 24 linhas de comentário do arquivo de *content signals* da Cloudflare, **sem uma linha de `User-agent`, `Disallow`, `Content-Signal` ou `Sitemap`** — nenhum buscador recebe o ponteiro para o `sitemap-index.xml`, que existe e responde 200.
2. O JSON-LD servido tem **apenas** `Organization`. A trilha de navegação existe na tela e em `src/lib/trilha.ts`, mas não há `BreadcrumbList`; não há `WebSite`, `WebPage` nem `VideoObject`.
3. `https://www.alupar.com.br/llms.txt` responde **404**.
4. Os números institucionais (45 sistemas, mais de 10 mil km, quase 800 MW, 4 países, rating AAA) existem **só em prosa** na Área de atuação, sem data de referência legível por máquina — e a data é justamente a pergunta aberta no item 4 do [pedido à Alupar](../../pedido-a-alupar-2026-09-21.md).

## Global Constraints

Copiadas do `AGENTS.md`. Valem para toda tarefa deste plano.

- **A linguagem visual é imutável.** Nenhuma tarefa deste plano muda pixel algum: tudo o que entra é `<head>`, arquivo de texto na raiz ou dado. Se uma tarefa exigir mudança visual, ela está errada.
- **Nenhuma URL do acervo pode responder 404.** Toda remoção vira 301 no mesmo commit. Este plano não remove nada.
- **Número não se inventa.** Onde faltar dado da Alupar, o lugar fica marcado como pendente. Os números da Tarefa 5 são os do texto que a Alupar enviou em 21/09/2026 (D19) e a data de observação é a desse envio — nenhum outro número entra.
- **Os critérios de aceite são condição de merge.** O CI reprova peso, acessibilidade, SEO, links quebrados e 404 do acervo. Nunca relaxe um limite para o CI passar.
- **Sem atribuição a ferramenta de IA** em commit, corpo de PR, comentário ou documento. Branches `feat/`, `fix/`, `docs/`, `chore/`.
- Código (variáveis, funções) em **inglês** quando for termo técnico consagrado; comentários, documentação e commits em **português**. Conventional commits.
- `npm test` roda `node --test "src/lib/*.test.ts" "scripts/lib/*.test.mjs"`. **Um teste só é executado se estiver num desses dois caminhos.**

---

## Estrutura de arquivos

| Arquivo | Responsabilidade | Tarefa |
|---|---|---|
| `public/robots.txt` | Política de rastreamento e ponteiro do sitemap | 1 |
| `scripts/lib/robots.test.mjs` | Prova que o robots tem sitemap, agente e sinal de conteúdo | 1 |
| `src/lib/seo.ts` | Constrói os objetos JSON-LD. Função pura, sem Astro | 2, 3, 5 |
| `src/lib/seo.test.ts` | Testes da biblioteca acima | 2, 3, 5 |
| `src/layouts/Base.astro` | Serializa o JSON-LD no `<head>` | 2, 3 |
| `src/pages/[...rota].astro` | Passa os degraus da trilha ao layout | 2 |
| `src/components/Home.astro` | Passa o vídeo institucional ao layout | 3 |
| `scripts/lib/llms.mjs` | Monta o conteúdo do `llms.txt` a partir do mapa de rotas | 4 |
| `scripts/lib/llms.test.mjs` | Testes do montador | 4 |
| `scripts/gerar-llms.mjs` | Escreve `public/llms.txt`; `--verificar` no CI | 4 |
| `public/llms.txt` | Artefato gerado, versionado | 4 |
| `.github/workflows/ci.yml` | Passo que reprova `llms.txt` desatualizado | 4 |
| `src/lib/numeros.ts` | Os números institucionais, com data de observação | 5 |
| `src/lib/numeros.test.ts` | Testes dos números | 5 |

---

### Task 1: `robots.txt` próprio, com sitemap e sinais de conteúdo

O arquivo que a Cloudflare serve hoje é só o preâmbulo explicativo dos *content signals*: define o vocabulário e **não usa nenhum sinal**, não declara agente e não aponta o sitemap. O nosso substitui esse arquivo mantendo o preâmbulo (é reserva de direitos sob a diretiva europeia, e a Alupar já o publica), e acrescenta o que falta.

**Decisão a confirmar com a Alupar, registrada aqui para não passar em silêncio:** o valor de `ai-train`. Este plano entra com `search=yes, ai-input=yes, ai-train=no` — que é a posição institucional corrente: aparecer em busca e em resposta de IA (é o objetivo do GEO), sem ceder o acervo para treinamento. Trocar o valor é editar uma linha.

**Files:**
- Create: `public/robots.txt`
- Test: `scripts/lib/robots.test.mjs`

**Interfaces:**
- Consumes: `astro.config.mjs` → `site: 'https://www.alupar.com.br'` (já existente)
- Produces: nada em código; produz o arquivo `/robots.txt` servido na raiz

- [ ] **Step 1: Escrever o teste que falha**

Crie `scripts/lib/robots.test.mjs`:

```javascript
/**
 * O robots.txt é o único arquivo do site que um buscador lê antes de tudo, e
 * era o que faltava: até 23/09/2026 o endereço servia só o preâmbulo dos
 * content signals da Cloudflare — vocabulário, sem nenhum sinal, sem agente e
 * sem o ponteiro do sitemap, que existe e responde 200.
 *
 * Estes testes olham o arquivo do repositório, não a rede: o que a borda serve
 * é conferido depois do deploy, pelo passo de verificação da Tarefa 6.
 */
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';

const robots = readFileSync('public/robots.txt', 'utf8');
const config = readFileSync('astro.config.mjs', 'utf8');
const site = /site:\s*'([^']+)'/.exec(config)?.[1];

test('o robots aponta o sitemap que o Astro gera, no domínio configurado', () => {
  assert.ok(site, 'astro.config.mjs sem `site` — o sitemap não teria domínio');
  assert.match(robots, new RegExp(`^Sitemap: ${site}/sitemap-index\\.xml$`, 'm'));
});

test('o robots declara um agente e libera o site', () => {
  assert.match(robots, /^User-agent: \*$/m);
  assert.match(robots, /^Allow: \/$/m);
});

test('os content signals são usados, não apenas explicados', () => {
  assert.match(robots, /^Content-Signal: search=yes, ai-input=yes, ai-train=no$/m);
});

test('o preâmbulo de reserva de direitos continua no arquivo', () => {
  assert.match(robots, /ARTICLE 4 OF THE EUROPEAN UNION DIRECTIVE 2019\/790/);
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Rode: `node --test scripts/lib/robots.test.mjs`
Esperado: FALHA com `ENOENT: no such file or directory, open 'public/robots.txt'`.

- [ ] **Step 3: Criar o arquivo**

Crie `public/robots.txt` com exatamente este conteúdo:

```
# As a condition of accessing this website, you agree to abide by the following
# content signals:

# (a)  If a content-signal = yes, you may collect content for the corresponding
#      use.
# (b)  If a content-signal = no, you may not collect content for the
#      corresponding use.
# (c)  If the website operator does not include a content signal for a
#      corresponding use, the website operator neither grants nor restricts
#      permission via content signal with respect to the corresponding use.

# The content signals and their meanings are:

# search:   building a search index and providing search results (e.g., returning
#           hyperlinks and short excerpts from your website's contents). Search does not
#           include providing AI-generated search summaries.
# ai-input: inputting content into one or more AI models (e.g., retrieval
#           augmented generation, grounding, or other real-time taking of content for
#           generative AI search answers).
# ai-train: training or fine-tuning AI models.

# ANY RESTRICTIONS EXPRESSED VIA CONTENT SIGNALS ARE EXPRESS RESERVATIONS OF
# RIGHTS UNDER ARTICLE 4 OF THE EUROPEAN UNION DIRECTIVE 2019/790 ON COPYRIGHT
# AND RELATED RIGHTS IN THE DIGITAL SINGLE MARKET.

User-agent: *
Content-Signal: search=yes, ai-input=yes, ai-train=no
Allow: /

Sitemap: https://www.alupar.com.br/sitemap-index.xml
```

- [ ] **Step 4: Rodar o teste e ver passar**

Rode: `node --test scripts/lib/robots.test.mjs`
Esperado: 4 testes, `pass 4`.

- [ ] **Step 5: Conferir que o arquivo entra no build**

Rode: `npm run build && cat dist/robots.txt | tail -3`
Esperado: as três últimas linhas são `Allow: /`, em branco e `Sitemap: https://www.alupar.com.br/sitemap-index.xml`.

- [ ] **Step 6: Commit**

```bash
git add public/robots.txt scripts/lib/robots.test.mjs
git commit -m "feat: robots.txt próprio, com sitemap e sinais de conteúdo"
```

---

### Task 2: `WebSite` e `BreadcrumbList` no JSON-LD

O JSON-LD hoje é um objeto literal dentro do `Base.astro`. Esta tarefa move a construção para biblioteca testada e acrescenta dois tipos: `WebSite` (identifica o site, o idioma e o domínio) e `BreadcrumbList` (a trilha que já existe na tela, legível por máquina).

**Files:**
- Create: `src/lib/seo.ts`
- Create: `src/lib/seo.test.ts`
- Modify: `src/layouts/Base.astro` (bloco do JSON-LD e a interface `Props`)
- Modify: `src/pages/[...rota].astro` (passa os degraus ao layout)

**Interfaces:**
- Consumes: `Degrau` e `degraus()` de `src/lib/trilha.ts` — `degraus(caminho: string, idioma: Idioma, titulo: string): Degrau[]`, onde `Degrau` é `{ rota?: string; titulo: string }`; `Idioma` e `lang` de `src/i18n/textos.ts`
- Produces: `organizacao()`, `siteWeb(idioma)`, `trilhaLd(degraus, origem)` e `grafo(...partes)` — consumidos pelas Tarefas 3 e 5 e pelo `Base.astro`

- [ ] **Step 1: Escrever o teste que falha**

Crie `src/lib/seo.test.ts`:

```typescript
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { organizacao, siteWeb, trilhaLd, grafo } from './seo.ts';

const ORIGEM = 'https://www.alupar.com.br';

test('a organização mantém o que já era servido, com o domínio absoluto', () => {
  const o = organizacao(ORIGEM);
  assert.equal(o['@type'], 'Organization');
  assert.equal(o.name, 'Alupar');
  assert.equal(o.url, ORIGEM);
  assert.equal(o.logo, `${ORIGEM}/logo-alupar.svg`);
});

test('o site declara idioma por rota: o português é o padrão, sem prefixo', () => {
  assert.equal(siteWeb(ORIGEM, 'pt-br').url, `${ORIGEM}/`);
  assert.equal(siteWeb(ORIGEM, 'pt-br').inLanguage, 'pt-BR');
  assert.equal(siteWeb(ORIGEM, 'en').url, `${ORIGEM}/en/`);
  assert.equal(siteWeb(ORIGEM, 'en').inLanguage, 'en');
  assert.equal(siteWeb(ORIGEM, 'es').inLanguage, 'es');
});

test('a trilha vira BreadcrumbList com posição de 1 em diante', () => {
  const l = trilhaLd([{ rota: '/', titulo: 'Início' }, { titulo: 'A Companhia' }], ORIGEM);
  assert.equal(l['@type'], 'BreadcrumbList');
  assert.deepEqual(l.itemListElement.map((i) => i.position), [1, 2]);
  assert.equal(l.itemListElement[0].name, 'Início');
  assert.equal(l.itemListElement[0].item, `${ORIGEM}/`);
});

test('o último degrau é a página atual e não leva item: ele não é link', () => {
  const l = trilhaLd([{ rota: '/', titulo: 'Início' }, { titulo: 'A Companhia' }], ORIGEM);
  assert.equal(l.itemListElement[1].item, undefined);
  assert.equal(l.itemListElement[1].name, 'A Companhia');
});

test('trilha com um degrau só não vira breadcrumb: não há caminho a descrever', () => {
  assert.equal(trilhaLd([{ titulo: 'Início' }], ORIGEM), null);
});

test('o grafo junta as partes e descarta o que for nulo', () => {
  const g = grafo(organizacao(ORIGEM), null, siteWeb(ORIGEM, 'pt-br'));
  assert.equal(g['@context'], 'https://schema.org');
  assert.deepEqual(g['@graph'].map((p) => p['@type']), ['Organization', 'WebSite']);
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Rode: `node --test src/lib/seo.test.ts`
Esperado: FALHA com `Cannot find module './seo.ts'`.

- [ ] **Step 3: Escrever a biblioteca**

Crie `src/lib/seo.ts`:

```typescript
/**
 * Os dados estruturados do site, como função pura.
 *
 * Ficavam num objeto literal dentro do `Base.astro`, com um tipo só
 * (`Organization`). Schema é regra, não marcação: aqui tem teste, e o template
 * só serializa o que esta biblioteca devolve.
 *
 * Tudo em `@graph`, e não em vários `<script>`: um grafo declara que as partes
 * descrevem a mesma página, que é o que elas fazem.
 */
import { lang, prefixo, type Idioma } from '../i18n/textos';

/** Objeto JSON-LD já pronto para serialização. */
export type Parte = Record<string, unknown> & { '@type': string };

export function organizacao(origem: string): Parte {
  return {
    '@type': 'Organization',
    name: 'Alupar',
    url: origem,
    logo: `${origem}/logo-alupar.svg`,
  };
}

export function siteWeb(origem: string, idioma: Idioma): Parte {
  return {
    '@type': 'WebSite',
    name: 'Alupar',
    url: `${origem}${prefixo[idioma]}/`,
    inLanguage: lang[idioma],
    publisher: { '@type': 'Organization', name: 'Alupar', url: origem },
  };
}

/**
 * A trilha da tela, em schema. O último degrau é a página atual: ele aparece
 * com nome e **sem** `item`, porque não é link — é onde a pessoa já está.
 *
 * Com um degrau só não há caminho a descrever, e um BreadcrumbList de um item
 * é ruído para o buscador: devolve `null`, que o `grafo` descarta.
 */
export function trilhaLd(passos: { rota?: string; titulo: string }[], origem: string): Parte | null {
  if (passos.length < 2) return null;
  return {
    '@type': 'BreadcrumbList',
    itemListElement: passos.map((passo, n) => ({
      '@type': 'ListItem',
      position: n + 1,
      name: passo.titulo,
      ...(passo.rota ? { item: `${origem}${passo.rota}` } : {}),
    })),
  };
}

export function grafo(...partes: (Parte | null)[]): Record<string, unknown> {
  return { '@context': 'https://schema.org', '@graph': partes.filter(Boolean) };
}
```

- [ ] **Step 4: Rodar o teste e ver passar**

Rode: `node --test src/lib/seo.test.ts`
Esperado: `pass 6`.

Se falhar em `lang` ou `prefixo`, confira os nomes exportados por `src/i18n/textos.ts` com `grep -n "export const" src/i18n/textos.ts` e ajuste o import — **não** duplique os mapas nesta biblioteca.

- [ ] **Step 5: Ligar no layout**

Em `src/layouts/Base.astro`, acrescente à interface `Props`:

```typescript
  /** Degraus da trilha, quando a página tem uma. Viram BreadcrumbList. */
  degraus?: { rota?: string; titulo: string }[];
```

Acrescente `degraus = []` à desestruturação de `Astro.props` e o import:

```typescript
import { organizacao, siteWeb, trilhaLd, grafo } from '../lib/seo';
```

Substitua o bloco do JSON-LD (o `<script type="application/ld+json">` com o objeto literal) por:

```astro
    <!-- JSON-LD no HTML servido, não injetado por JavaScript como no site antigo.
         O que cada parte diz, e por que, está em src/lib/seo.ts. -->
    <script type="application/ld+json" set:html={JSON.stringify(grafo(
      organizacao(Astro.site!.origin),
      siteWeb(Astro.site!.origin, idioma),
      trilhaLd(degraus, Astro.site!.origin),
    ))} />
```

- [ ] **Step 6: Passar os degraus na rota do acervo**

Em `src/pages/[...rota].astro`, acrescente o import e o cálculo:

```typescript
import { degraus } from '../lib/trilha.ts';
```

Depois de `const t = textos[idioma];`:

```typescript
/* Os mesmos degraus que a Trilha desenha, para o BreadcrumbList do layout — de
   uma fonte só, senão a tela e o schema divergem no primeiro título que mudar. */
const passos = degraus(Astro.url.pathname, idioma, item.titulo);
```

E passe ao layout, na abertura do `<Base ...>`:

```astro
<Base titulo={`${item.titulo} — Alupar`} descricao={t.descricao} idioma={idioma} noindex={ehCasca(item)} degraus={passos}>
```

- [ ] **Step 7: Conferir no build**

Rode:

```bash
npm run build
node -e "const h=require('fs').readFileSync('dist/a-companhia/index.html','utf8');const m=/<script type=\"application\/ld\+json\">([^<]+)</.exec(h);console.log(JSON.stringify(JSON.parse(m[1])['@graph'].map(p=>p['@type'])));"
```

Esperado: `["Organization","WebSite","BreadcrumbList"]`.

Rode também: `node -e "const h=require('fs').readFileSync('dist/index.html','utf8');const m=/<script type=\"application\/ld\+json\">([^<]+)</.exec(h);console.log(JSON.stringify(JSON.parse(m[1])['@graph'].map(p=>p['@type'])));"`

Esperado: `["Organization","WebSite"]` — a home não tem trilha, e o `trilhaLd` devolveu `null`.

- [ ] **Step 8: Rodar os portões locais**

Rode: `npm test && node scripts/verificar-csp.mjs && node scripts/verificar-links.mjs`
Esperado: testes verdes, CSP aprovada, links aprovados.

- [ ] **Step 9: Commit**

```bash
git add src/lib/seo.ts src/lib/seo.test.ts src/layouts/Base.astro "src/pages/[...rota].astro"
git commit -m "feat: WebSite e BreadcrumbList no JSON-LD, com a trilha que já existe"
```

---

### Task 3: `WebPage` e `VideoObject`

`WebPage` dá à página um nó próprio, com idioma, título e descrição — é o que amarra o `BreadcrumbList` à página certa. `VideoObject` descreve o institucional da home, que hoje é um `iframe` do YouTube sem nenhuma descrição legível por máquina.

**Files:**
- Modify: `src/lib/seo.ts`
- Modify: `src/lib/seo.test.ts`
- Modify: `src/layouts/Base.astro`
- Modify: `src/components/Home.astro`

**Interfaces:**
- Consumes: `Parte`, `grafo` da Tarefa 2; `VIDEO_INSTITUCIONAL` de `src/lib/videos.ts` (string com o id do YouTube)
- Produces: `paginaWeb({ origem, url, titulo, descricao, idioma })` e `video({ id, nome, descricao, origem })`

- [ ] **Step 1: Escrever os testes que falham**

Acrescente ao fim de `src/lib/seo.test.ts`:

```typescript
test('a página web declara url, idioma e o site a que pertence', () => {
  const p = paginaWeb({
    origem: ORIGEM, url: `${ORIGEM}/a-companhia/`,
    titulo: 'A Companhia — Alupar', descricao: 'Texto da descrição', idioma: 'pt-br',
  });
  assert.equal(p['@type'], 'WebPage');
  assert.equal(p.url, `${ORIGEM}/a-companhia/`);
  assert.equal(p.inLanguage, 'pt-BR');
  assert.equal(p.name, 'A Companhia — Alupar');
  assert.equal(p.description, 'Texto da descrição');
  assert.deepEqual(p.isPartOf, { '@type': 'WebSite', url: `${ORIGEM}/` });
});

test('o vídeo aponta a página de reprodução e a miniatura do YouTube', () => {
  const v = video({ id: 'oqjwsKfpYZ4', nome: 'Vídeo institucional', descricao: 'Alupar', origem: ORIGEM });
  assert.equal(v['@type'], 'VideoObject');
  assert.equal(v.embedUrl, 'https://www.youtube-nocookie.com/embed/oqjwsKfpYZ4');
  assert.equal(v.thumbnailUrl, 'https://i.ytimg.com/vi/oqjwsKfpYZ4/maxresdefault.jpg');
  assert.equal(v.name, 'Vídeo institucional');
});
```

E acrescente `paginaWeb, video` ao import do topo do arquivo.

- [ ] **Step 2: Rodar e ver falhar**

Rode: `node --test src/lib/seo.test.ts`
Esperado: FALHA com `paginaWeb is not a function` (ou erro de import).

- [ ] **Step 3: Implementar**

Acrescente a `src/lib/seo.ts`:

```typescript
/**
 * O nó da página. Existe para o BreadcrumbList ter a que se referir e para o
 * idioma da página ficar declarado também no grafo, não só no `<html lang>`.
 */
export function paginaWeb(
  { origem, url, titulo, descricao, idioma }:
  { origem: string; url: string; titulo: string; descricao: string; idioma: Idioma },
): Parte {
  return {
    '@type': 'WebPage',
    url,
    name: titulo,
    description: descricao,
    inLanguage: lang[idioma],
    isPartOf: { '@type': 'WebSite', url: `${origem}/` },
  };
}

/**
 * O institucional da home. O `embedUrl` usa o domínio sem cookies, que é o que
 * o site carrega e o que a CSP permite; a miniatura é a do próprio YouTube, e
 * não a nossa capa local, porque é ela que o buscador consegue casar com o
 * vídeo.
 */
export function video(
  { id, nome, descricao, origem }: { id: string; nome: string; descricao: string; origem: string },
): Parte {
  return {
    '@type': 'VideoObject',
    name: nome,
    description: descricao,
    embedUrl: `https://www.youtube-nocookie.com/embed/${id}`,
    thumbnailUrl: `https://i.ytimg.com/vi/${id}/maxresdefault.jpg`,
    publisher: { '@type': 'Organization', name: 'Alupar', url: origem },
  };
}
```

- [ ] **Step 4: Rodar e ver passar**

Rode: `node --test src/lib/seo.test.ts`
Esperado: `pass 8`.

- [ ] **Step 5: Ligar no layout**

Em `src/layouts/Base.astro`, acrescente à interface `Props`:

```typescript
  /** Partes extras do grafo — hoje o VideoObject da home. */
  extras?: { '@type': string }[];
```

Acrescente `extras = []` à desestruturação, troque o import para incluir `paginaWeb`, e o bloco do JSON-LD para:

```astro
    <script type="application/ld+json" set:html={JSON.stringify(grafo(
      organizacao(Astro.site!.origin),
      siteWeb(Astro.site!.origin, idioma),
      paginaWeb({ origem: Astro.site!.origin, url, titulo, descricao, idioma }),
      trilhaLd(degraus, Astro.site!.origin),
      ...extras,
    ))} />
```

`url`, `titulo` e `descricao` já existem no escopo do componente — confira com `grep -n "const url" src/layouts/Base.astro` antes de escrever.

- [ ] **Step 6: Ligar na home**

Em `src/components/Home.astro`, acrescente ao frontmatter:

```typescript
import { video } from '../lib/seo';
```

E, na abertura do `<Base ...>`, passe o vídeo:

```astro
<Base titulo={t.titulo} descricao={t.descricao} idioma={idioma} interna={false}
  extras={[video({ id: VIDEO_INSTITUCIONAL, nome: t.videoInstitucional, descricao: t.descricao, origem: Astro.site!.origin })]}>
```

- [ ] **Step 7: Conferir no build**

Rode:

```bash
npm run build
node -e "const h=require('fs').readFileSync('dist/index.html','utf8');const m=/<script type=\"application\/ld\+json\">([^<]+)</.exec(h);console.log(JSON.stringify(JSON.parse(m[1])['@graph'].map(p=>p['@type'])));"
```

Esperado: `["Organization","WebSite","WebPage","VideoObject"]`.

- [ ] **Step 8: Rodar os portões locais**

Rode: `npm test && npm run build && node scripts/verificar-csp.mjs`
Esperado: tudo verde. A CSP não muda: o JSON-LD é bloco de dados, não script executável.

- [ ] **Step 9: Commit**

```bash
git add src/lib/seo.ts src/lib/seo.test.ts src/layouts/Base.astro src/components/Home.astro
git commit -m "feat: WebPage em toda página e VideoObject no institucional da home"
```

---

### Task 4: `llms.txt` gerado do mapa de rotas, com verificação no CI

`https://www.alupar.com.br/llms.txt` responde 404. O arquivo diz, em texto, o que a Alupar é e onde está cada assunto — é o que um modelo lê para citar a empresa sem inventar. Escrito à mão, envelhece no primeiro menu que mudar; por isso é **gerado** do mapa de rotas e conferido no CI, como os outros artefatos derivados deste repositório.

**Files:**
- Create: `scripts/lib/llms.mjs`
- Create: `scripts/lib/llms.test.mjs`
- Create: `scripts/gerar-llms.mjs`
- Create: `public/llms.txt` (gerado no Step 5)
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: `acervo/mapa-de-rotas.json` (`{ rotas: [{ rota, idioma, tipo, titulo }] }`)
- Produces: `montar(mapa, { origem })` → string com o conteúdo do `llms.txt`

- [ ] **Step 1: Escrever o teste que falha**

Crie `scripts/lib/llms.test.mjs`:

```javascript
/**
 * O llms.txt é a fonte canônica que um modelo lê para citar a Alupar. Escrito à
 * mão, envelhece no primeiro menu que mudar — daí ser gerado do mapa de rotas e
 * conferido no CI.
 */
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { montar } from './llms.mjs';

const MAPA = {
  rotas: [
    { rota: '/', idioma: 'pt', tipo: 'pagina', titulo: 'Início' },
    { rota: '/a-companhia/', idioma: 'pt', tipo: 'pagina', titulo: 'A Companhia' },
    { rota: '/area-de-atuacao/', idioma: 'pt', tipo: 'pagina', titulo: 'Área de atuação' },
    { rota: '/faq/algo/', idioma: 'pt', tipo: 'faq', titulo: 'Algo' },
    { rota: '/en/a-companhia/', idioma: 'en', tipo: 'pagina', titulo: 'Company' },
  ],
};

test('abre com o nome e uma frase do que a Alupar é', () => {
  const txt = montar(MAPA, { origem: 'https://www.alupar.com.br' });
  assert.match(txt, /^# Alupar$/m);
  assert.match(txt, /transmiss(ã|a)o/i);
});

test('lista as páginas em português, com URL absoluta', () => {
  const txt = montar(MAPA, { origem: 'https://www.alupar.com.br' });
  assert.match(txt, /- \[A Companhia\]\(https:\/\/www\.alupar\.com\.br\/a-companhia\/\)/);
});

test('não lista as versões em inglês e espanhol: o llms.txt é um, em português', () => {
  const txt = montar(MAPA, { origem: 'https://www.alupar.com.br' });
  assert.doesNotMatch(txt, /\/en\/a-companhia\//);
});

test('não lista FAQ: são 101 itens e afogariam as páginas institucionais', () => {
  const txt = montar(MAPA, { origem: 'https://www.alupar.com.br' });
  assert.doesNotMatch(txt, /\/faq\/algo\//);
});

test('aponta as fontes canônicas que não são deste site', () => {
  const txt = montar(MAPA, { origem: 'https://www.alupar.com.br' });
  for (const host of ['ri.alupar.com.br', 'rs.alupar.com.br', 'pdi.alupar.com.br']) {
    assert.ok(txt.includes(host), `llms.txt sem ${host}`);
  }
});

test('o mesmo mapa gera o mesmo texto: sem data de geração, o arquivo não muda à toa', () => {
  const a = montar(MAPA, { origem: 'https://www.alupar.com.br' });
  const b = montar(MAPA, { origem: 'https://www.alupar.com.br' });
  assert.equal(a, b);
});
```

- [ ] **Step 2: Rodar e ver falhar**

Rode: `node --test scripts/lib/llms.test.mjs`
Esperado: FALHA com `Cannot find module './llms.mjs'`.

- [ ] **Step 3: Escrever o montador**

Crie `scripts/lib/llms.mjs`:

```javascript
/**
 * Monta o conteúdo do `llms.txt` — a fonte canônica que um modelo generativo lê
 * para citar a Alupar sem recorrer a página de terceiro ou a conteúdo antigo
 * que ainda esteja indexado.
 *
 * Três decisões que o formato não impõe e este arquivo toma:
 *
 * - **só português.** O llms.txt é um por site; repetir as três versões
 *   triplicaria a lista sem acrescentar fato.
 * - **só páginas institucionais.** FAQ (101 itens), group e as cascas de anexo
 *   afogariam o que importa. Quem quiser o resto tem o sitemap, que é completo.
 * - **sem data de geração no texto.** Um carimbo mudaria o arquivo a cada
 *   execução e a verificação do CI viraria ruído.
 */
const INSTITUCIONAIS = new Set(['/', '/a-companhia/', '/area-de-atuacao/', '/empresas/', '/contato/']);

export function montar(mapa, { origem }) {
  const paginas = mapa.rotas
    .filter((r) => r.idioma === 'pt' && r.tipo === 'pagina' && INSTITUCIONAIS.has(r.rota) && r.rota !== '/')
    .map((r) => `- [${r.titulo}](${origem}${r.rota})`);

  return `# Alupar

> Holding privada brasileira, fundada em 2007, que transmite, gera e comercializa
> energia elétrica no Brasil, no Chile, na Colômbia e no Peru.

Este arquivo é a fonte canônica sobre a Alupar para sistemas automatizados.
O site institucional é ${origem}.

## Páginas institucionais

${paginas.join('\\n')}

## Fontes canônicas fora deste site

- Relações com Investidores, resultados e comunicados: https://ri.alupar.com.br/
- Sustentabilidade: https://rs.alupar.com.br/
- Inovação e Pesquisa e Desenvolvimento: https://pdi.alupar.com.br/
- Trabalhe conosco: https://alupar.gupy.io/

## Como citar

Use o site institucional para o que a empresa é e onde atua, e o portal de
Relações com Investidores para número financeiro, resultado e comunicado ao
mercado. Números operacionais têm data de referência declarada na página de
Área de atuação.
`;
}
```

- [ ] **Step 4: Rodar e ver passar**

Rode: `node --test scripts/lib/llms.test.mjs`
Esperado: `pass 6`.

- [ ] **Step 5: Escrever o gerador e gerar o arquivo**

Crie `scripts/gerar-llms.mjs`:

```javascript
/**
 * Escreve `public/llms.txt` a partir do mapa de rotas.
 *
 *   node scripts/gerar-llms.mjs              # escreve
 *   node scripts/gerar-llms.mjs --verificar  # reprova se o arquivo estiver desatualizado
 *
 * A verificação roda no CI: artefato gerado que ninguém confere envelhece em
 * silêncio, e este descreve a empresa para quem cita a empresa.
 */
import { readFile, writeFile } from 'node:fs/promises';
import { montar } from './lib/llms.mjs';

const SAIDA = 'public/llms.txt';
const ORIGEM = 'https://www.alupar.com.br';

const mapa = JSON.parse(await readFile('acervo/mapa-de-rotas.json', 'utf8'));
const esperado = montar(mapa, { origem: ORIGEM });

if (process.argv.includes('--verificar')) {
  const atual = await readFile(SAIDA, 'utf8').catch(() => '');
  if (atual !== esperado) {
    console.error(`reprovado: ${SAIDA} está desatualizado — rode \`node scripts/gerar-llms.mjs\``);
    process.exit(1);
  }
  console.log(`aprovado: ${SAIDA} corresponde ao mapa de rotas.`);
} else {
  await writeFile(SAIDA, esperado);
  console.log(`escrito: ${SAIDA} (${esperado.split('\n').length} linhas)`);
}
```

Rode: `node scripts/gerar-llms.mjs && node scripts/gerar-llms.mjs --verificar`
Esperado: `escrito: public/llms.txt (…)` e depois `aprovado: …`.

- [ ] **Step 6: Ligar no CI**

Em `.github/workflows/ci.yml`, no job `build`, logo **antes** do passo `- run: npm run build`, acrescente:

```yaml
      - name: llms.txt corresponde ao mapa de rotas
        run: node scripts/gerar-llms.mjs --verificar
```

- [ ] **Step 7: Conferir que o arquivo é servido**

Rode: `npm run build && head -3 dist/llms.txt`
Esperado: a primeira linha é `# Alupar`.

- [ ] **Step 8: Commit**

```bash
git add scripts/lib/llms.mjs scripts/lib/llms.test.mjs scripts/gerar-llms.mjs public/llms.txt .github/workflows/ci.yml
git commit -m "feat: llms.txt gerado do mapa de rotas, conferido no CI"
```

---

### Task 5: números institucionais como fato extraível, com data de observação

Os números vivem só em prosa. Esta tarefa os transforma em dado versionado, com **data de observação**, e os publica no `Organization` como `QuantitativeValue`. Nenhum texto da página muda: o que entra é schema.

A data é **21/09/2026**, dia em que a Alupar enviou o texto revisado (D19). Não é a data de apuração dos números — essa é a pergunta aberta no item 4 do pedido à Comunicação. Enquanto ela não chega, declarar a data do envio é o que se pode afirmar sem inventar, e o comentário do arquivo diz isso.

**Files:**
- Create: `src/lib/numeros.ts`
- Create: `src/lib/numeros.test.ts`
- Modify: `src/lib/seo.ts` (a `organizacao` passa a receber os números)
- Modify: `src/lib/seo.test.ts`

**Interfaces:**
- Consumes: nada
- Produces: `NUMEROS` — `{ chave: string; valor: number; unidade: string; rotulo: string }[]` — e `OBSERVADO_EM: string` (ISO 8601)

- [ ] **Step 1: Escrever o teste que falha**

Crie `src/lib/numeros.test.ts`:

```typescript
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { NUMEROS, OBSERVADO_EM } from './numeros.ts';

test('os quatro números do texto de 21/09/2026, e nenhum a mais', () => {
  assert.deepEqual(NUMEROS.map((n) => n.chave), ['sistemas', 'linhas', 'capacidade', 'paises']);
});

test('cada número tem valor, unidade e rótulo legível', () => {
  for (const n of NUMEROS) {
    assert.equal(typeof n.valor, 'number', `${n.chave} sem valor numérico`);
    assert.ok(n.unidade.length > 0, `${n.chave} sem unidade`);
    assert.ok(n.rotulo.length > 0, `${n.chave} sem rótulo`);
  }
});

test('a data de observação é a do texto que a Alupar enviou, em ISO', () => {
  assert.equal(OBSERVADO_EM, '2026-09-21');
});
```

- [ ] **Step 2: Rodar e ver falhar**

Rode: `node --test src/lib/numeros.test.ts`
Esperado: FALHA com `Cannot find module './numeros.ts'`.

- [ ] **Step 3: Escrever os números**

Crie `src/lib/numeros.ts`:

```typescript
/**
 * Os números institucionais, como dado.
 *
 * Eles existiam só em prosa, na Área de atuação. Em prosa, um modelo os cita
 * sem data e um buscador não os entende — e "quase 800 MW" sem referência
 * temporal envelhece sem que ninguém perceba.
 *
 * **A data.** `OBSERVADO_EM` é o dia em que a Alupar enviou o texto revisado
 * (D19), e não a data de apuração: essa é a pergunta aberta no item 4 do
 * `docs/pedido-a-alupar-2026-09-21.md`. Declarar o que se sabe é honesto;
 * declarar uma apuração que ninguém confirmou seria inventar. Quando a
 * Comunicação responder, muda-se uma linha.
 *
 * Os valores são exatamente os do texto enviado. Nenhum é arredondado aqui.
 */
export const OBSERVADO_EM = '2026-09-21';

export const NUMEROS = [
  { chave: 'sistemas', valor: 45, unidade: 'sistemas de transmissão', rotulo: 'Sistemas de transmissão' },
  { chave: 'linhas', valor: 10000, unidade: 'km', rotulo: 'Linhas de transmissão' },
  { chave: 'capacidade', valor: 800, unidade: 'MW', rotulo: 'Capacidade instalada' },
  { chave: 'paises', valor: 4, unidade: 'países', rotulo: 'Países de atuação' },
] as const;
```

- [ ] **Step 4: Rodar e ver passar**

Rode: `node --test src/lib/numeros.test.ts`
Esperado: `pass 3`.

- [ ] **Step 5: Escrever o teste do schema**

Acrescente a `src/lib/seo.test.ts` (e acrescente `OBSERVADO_EM` ao import de `./numeros.ts`):

```typescript
test('a organização publica os números como QuantitativeValue, com data de observação', () => {
  const props = organizacao(ORIGEM).additionalProperty as Record<string, unknown>[];
  assert.equal(props.length, 4);
  const linhas = props.find((p) => p.name === 'Linhas de transmissão');
  assert.equal(linhas?.['@type'], 'PropertyValue');
  assert.deepEqual(linhas?.value, { '@type': 'QuantitativeValue', value: 10000, unitText: 'km' });
  assert.equal(linhas?.observationDate, '2026-09-21');
});
```

- [ ] **Step 6: Rodar e ver falhar**

Rode: `node --test src/lib/seo.test.ts`
Esperado: FALHA — `additionalProperty` é `undefined`.

- [ ] **Step 7: Implementar**

Em `src/lib/seo.ts`, acrescente o import e o campo:

```typescript
import { NUMEROS, OBSERVADO_EM } from './numeros';
```

E dentro de `organizacao`, depois de `logo`:

```typescript
    /* Os números do texto institucional, legíveis por máquina e com data de
       observação — em prosa eles não são citáveis sem risco de envelhecer. */
    additionalProperty: NUMEROS.map((n) => ({
      '@type': 'PropertyValue',
      name: n.rotulo,
      value: { '@type': 'QuantitativeValue', value: n.valor, unitText: n.unidade },
      observationDate: OBSERVADO_EM,
    })),
```

- [ ] **Step 8: Rodar e ver passar**

Rode: `npm test`
Esperado: todos verdes, com os três arquivos novos de teste somados ao total.

- [ ] **Step 9: Conferir no build**

Rode:

```bash
npm run build
node -e "const h=require('fs').readFileSync('dist/index.html','utf8');const m=/<script type=\"application\/ld\+json\">([^<]+)</.exec(h);const o=JSON.parse(m[1])['@graph'].find(p=>p['@type']==='Organization');console.log(o.additionalProperty.map(p=>p.name+': '+p.value.value+' '+p.value.unitText).join(' | '));"
```

Esperado: `Sistemas de transmissão: 45 sistemas de transmissão | Linhas de transmissão: 10000 km | Capacidade instalada: 800 MW | Países de atuação: 4 países`.

- [ ] **Step 10: Commit**

```bash
git add src/lib/numeros.ts src/lib/numeros.test.ts src/lib/seo.ts src/lib/seo.test.ts
git commit -m "feat: números institucionais como dado, com data de observação no schema"
```

---

### Task 6: verificação no ar e registro

O plano só termina quando o que foi implementado está servido e conferido **no domínio de produção**, não no build local.

**Files:**
- Modify: `docs/decisoes.md` (D28)
- Modify: `docs/status.md` (tabela de setembro)

**Interfaces:**
- Consumes: tudo o que as Tarefas 1 a 5 produziram
- Produces: nada em código

- [ ] **Step 1: Abrir o PR e esperar o verde**

```bash
git push -u origin feat/seo-e-geo
gh pr create --base main --title "SEO e GEO: robots.txt, dados estruturados, llms.txt e números com data"
```

Espere o `qualidade` fechar (~12 min). **Não mergeie com o portão amarelo.**

- [ ] **Step 2: Mergear e esperar a publicação**

```bash
gh pr merge --squash --delete-branch
gh run watch $(gh run list --branch main --limit 1 --json databaseId --jq '.[0].databaseId') --exit-status
```

- [ ] **Step 3: Conferir no ar**

```bash
curl -s https://www.alupar.com.br/robots.txt | grep -E "^(User-agent|Content-Signal|Allow|Sitemap)"
curl -s -o /dev/null -w "llms.txt: %{http_code}\n" https://www.alupar.com.br/llms.txt
curl -s https://www.alupar.com.br/a-companhia/ | node -e "let h='';process.stdin.on('data',d=>h+=d).on('end',()=>{const m=/<script type=\"application\/ld\+json\">([^<]+)</.exec(h);console.log(JSON.parse(m[1])['@graph'].map(p=>p['@type']).join(', '));})"
```

Esperado: as quatro diretivas do robots; `llms.txt: 200`; e `Organization, WebSite, WebPage, BreadcrumbList`.

**Se o `robots.txt` no ar não for o nosso**, a funcionalidade de *AI content signals* da Cloudflare está sobrepondo o arquivo do projeto: desligue-a no painel da zona (Security → Settings → AI content signals) e repita a conferência. O arquivo do repositório já contém o preâmbulo dela, então nada se perde.

- [ ] **Step 4: Enviar o sitemap aos buscadores**

No Google Search Console e no Bing Webmaster Tools, envie `https://www.alupar.com.br/sitemap-index.xml`. Não é passo de código, e é o que fecha a entrega: sem ele, o `robots.txt` aponta o sitemap para quem já encontrou o site.

- [ ] **Step 5: Registrar a decisão**

Acrescente a `docs/decisoes.md`, depois da D27:

```markdown
| D28 | SEO técnico e GEO | **`robots.txt` próprio com sinais de conteúdo, JSON-LD em grafo (Organization, WebSite, WebPage, BreadcrumbList, VideoObject), `llms.txt` gerado e números com data de observação** | Medido em 24/09/2026, um dia depois da virada: o `robots.txt` servido era só o preâmbulo dos content signals — sem agente, sem sinal e sem ponteiro para o sitemap, que existe e responde 200; o JSON-LD tinha só `Organization`; o `llms.txt` respondia 404; e os números institucionais viviam só em prosa, sem data. Os sinais entram como `search=yes, ai-input=yes, ai-train=no`: aparecer em busca e em resposta de IA é o objetivo, ceder o acervo para treinamento não. A data de observação dos números é a do envio do texto pela Alupar (D19), e não a de apuração — essa segue pendente no item 4 do pedido à Comunicação |
```

E acrescente a linha à tabela de `docs/status.md`.

- [ ] **Step 6: Commit**

```bash
git add docs/decisoes.md docs/status.md
git commit -m "docs: D28 registra o SEO técnico e o GEO"
```

---

## Autorrevisão

**Cobertura do spec.** Achado 1 → Tarefa 1. Achado 2 → Tarefas 2 e 3. Achado 3 → Tarefa 4. Achado 4 → Tarefa 5. Verificação no ar e registro → Tarefa 6. Sem lacuna.

**Marcadores.** Nenhum "TBD", "implementar depois" ou "tratar os casos de borda". Todo passo de código traz o código. A única incógnita do plano — o valor de `ai-train` e a data de apuração dos números — está escrita como decisão pendente **com um valor concreto em vigor até a resposta**, e não como lacuna.

**Consistência de tipos.** `Parte` é definido na Tarefa 2 e usado nas 3 e 5. `organizacao(origem)` ganha um campo na Tarefa 5 sem mudar de assinatura. `degraus()` e `Degrau` vêm de `src/lib/trilha.ts` com a assinatura que o arquivo já tem. `NUMEROS`/`OBSERVADO_EM` são produzidos na Tarefa 5 e consumidos só ali e em `seo.ts`. `VIDEO_INSTITUCIONAL` é o nome exportado hoje por `src/lib/videos.ts`.

**Risco conhecido.** O `Astro.site!.origin` exige `site` no `astro.config.mjs` — ele está definido (`https://www.alupar.com.br`). Se algum dia sair, o build quebra em vez de gerar URL relativa errada, que é o comportamento desejável.
