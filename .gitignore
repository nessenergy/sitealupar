node_modules/
dist/
.astro/
.env
.env.*
!.env.example
.DS_Store
npm-debug.log*
.lighthouseci/

# Extração do acervo: os inventários são versionados, os arquivos não.
acervo-completo/
acervo/html/
acervo/midia/

# mídia extraída dos microsites — 94,7 MB de binários, fora do git
acervo/microsites/*/midia/

# acervo do institucional — 283 MB de HTML e binários, fora do git
acervo/html/
acervo/midia/

# gerados a partir de apresentacao/deck.js e dossie.html
apresentacao/*.pptx
apresentacao/*.pdf
apresentacao/node_modules/
apresentacao/package-lock.json

# capturas da conferência visual (scripts/capturar-telas.mjs) — binário, fora do git
capturas/

# cache do `wrangler pages dev`, usado para conferir o build local antes do
# deploy — é gerado, não versionado
.wrangler/

# Configuração pessoal do Claude Code: permissões desta máquina, não do projeto
.claude/settings.local.json
