---
import type { GetStaticPaths } from 'astro';
import ListaNoticias from '../../../../components/ListaNoticias.astro';
import { noticiasDe, POR_PAGINA } from '../../../../lib/acervo';

export const getStaticPaths = (({ paginate }) => paginate(noticiasDe('en'), { pageSize: POR_PAGINA })) satisfies GetStaticPaths;
const { page } = Astro.props;
---
<ListaNoticias idioma="en" lista={page.data} pagina={page} />
